<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title> <!-- Dynamic title -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;700&family=Roboto+Mono:wght@300;400&display=swap" rel="stylesheet">
    <style>
        :root {
            --rp-green: #006341;
            --rp-green-light: #008050;
            --rp-green-dark: #004d33;
            --rp-black: #121212;
            --rp-white: #ffffff;
            --rp-gray: #f5f5f5;
            --rp-light-gray: #e0e0e0;
        }
        
        body {
            margin: 0;
            padding: 0;
            font-family: 'Montserrat', sans-serif;
            background-color: var(--rp-gray);
            color: var(--rp-black);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .business-tech-container {
            width: 100%;
            max-width: 1200px;
            display: flex;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            border-radius: 12px;
            overflow: hidden;
            height: 600px;
        }

        .tech-panel {
            flex: 1;
            background: var(--rp-black);
            padding: 60px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .tech-panel::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                linear-gradient(135deg, rgba(0, 99, 65, 0.1) 0%, transparent 50%),
                linear-gradient(-135deg, rgba(0, 99, 65, 0.1) 0%, transparent 50%);
            z-index: 1;
        }

        .login-panel {
            flex: 1;
            background: var(--rp-white);
            padding: 60px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .tech-logo {
            position: relative;
            z-index: 2;
            margin-bottom: 40px;
        }

        .tech-logo svg {
            width: 180px;
        }

        .tech-headline {
            position: relative;
            z-index: 2;
            color: var(--rp-white);
            margin-bottom: 30px;
        }

        .tech-headline h2 {
            font-size: 28px;
            font-weight: 600;
            margin-bottom: 15px;
        }

        .tech-headline p {
            font-family: 'Roboto Mono', monospace;
            font-size: 14px;
            opacity: 0.8;
            line-height: 1.6;
        }

        .tech-stats {
            position: relative;
            z-index: 2;
            display: flex;
            margin-top: 40px;
        }
        .stat-item {
            margin-right: 30px;
        }

        .stat-number {
            font-size: 28px;
            font-weight: 700;
            color: var(--rp-green);
            margin-bottom: 5px;
        }

        .stat-label {
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 1px;
            opacity: 0.7;
            color: var(--rp-white);
        }

        .login-header {
            margin-bottom: 40px;
        }

        .login-header h1 {
            font-size: 28px;
            font-weight: 700;
            color: var(--rp-black);
            margin-bottom: 10px;
        }

        .login-header p {
            color: #666;
            font-size: 14px;
        }

        .login-form {
            width: 100%;
            max-width: 400px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-size: 13px;
            font-weight: 600;
            color: var(--rp-black);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .form-control {
            width: 100%;
            padding: 14px 16px;
            border: 1px solid var(--rp-light-gray);
            border-radius: 6px;
            font-family: 'Montserrat', sans-serif;
            font-size: 14px;
            transition: all 0.3s;
            background-color: var(--rp-gray);
        }

        .form-control:focus {
            outline: none;
            border-color: var(--rp-green);
            box-shadow: 0 0 0 2px rgba(0, 99, 65, 0.2);
            background-color: var(--rp-white);
        }

        .btn-login {
            width: 100%;
            padding: 16px;
            background-color: var(--rp-green);
            color: var(--rp-white);
            border: none;
            border-radius: 6px;
            font-family: 'Montserrat', sans-serif;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            margin-top: 10px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .btn-login:hover {
            background-color: var(--rp-green-dark);
            transform: translateY(-2px);
        }

        .btn-register {
            width: 100%;
            padding: 16px;
            background-color: transparent;
            color: var(--rp-green);
            border: 2px solid var(--rp-green);
            border-radius: 6px;
            font-family: 'Montserrat', sans-serif;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            margin-top: 15px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .btn-register:hover {
            background-color: rgba(0, 99, 65, 0.1);
            transform: translateY(-2px);
        }

        .login-footer {
            margin-top: 30px;
            font-size: 13px;
            color: #666;
            text-align: center;
        }

        .login-footer a {
            color: var(--rp-green);
            text-decoration: none;
            font-weight: 600;
        }

        .forgot-link {
            text-align: center;
            margin-top: 15px;
            font-size: 13px;
        }

        .forgot-link a {
            color: var(--rp-green);
            text-decoration: none;
            font-weight: 600;
        }

        .forgot-link a:hover {
            text-decoration: underline;
        }

        .tech-grid {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(rgba(0, 99, 65, 0.05) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 99, 65, 0.05) 1px, transparent 1px);
            background-size: 40px 40px;
            z-index: 1;
        }
        .tech-circle {
            position: absolute;
            border-radius: 50%;
            background: rgba(0, 99, 65, 0.1);
            z-index: 1;
        }

        .circle-1 {
            width: 300px;
            height: 300px;
            top: -100px;
            right: -100px;
        }

        .circle-2 {
            width: 150px;
            height: 150px;
            bottom: 50px;
            left: 50px;
        }

        .button-group {
            display: flex;
            flex-direction: column;
            margin-top: 20px;
        }

        .account-prompt {
            text-align: center;
            margin-top: 15px;
            font-size: 13px;
            color: #666;
        }

        .account-prompt a {
            color: var(--rp-green);
            text-decoration: none;
            font-weight: 600;
            margin-left: 5px;
        }

        .account-prompt a:hover {
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            .business-tech-container {
                flex-direction: column;
                height: auto;
            }
            
            .tech-panel, .login-panel {
                padding: 40px;
            }
        }
    </style>
</head>
<body>
    <div class="business-tech-container">
        <div class="tech-panel">
            <div class="tech-grid"></div>
            <div class="tech-circle circle-1"></div>
            <div class="tech-circle circle-2"></div>
            
            <div class="tech-logo">
                <svg viewBox="0 0 265 60" xmlns="http://www.w3.org/2000/svg">
                    <text x="0" y="45" font-family="Montserrat" font-size="40" font-weight="700" fill="#ffffff">RPCONNECT</text>
                </svg>
            </div>
            
            <div class="tech-headline">
                <h2>RPConnect – Where Republic Poly Students Speak Freely</h2>
                <p>RPConnect is a student-led forum created for everyone at Republic Polytechnic. It’s a safe space where you can share your thoughts, speak your mind, and connect with fellow RP students from all walks of campus life.</p>
            </div>
            
            <div class="tech-stats">
                <div class="stat-item">
                    <div class="stat-number">15K+</div>
                    <div class="stat-label">ACTIVE USERS</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number">24/7</div>
                    <div class="stat-label">UPTIME</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number">99.9%</div>
                    <div class="stat-label">RELIABILITY</div>
                </div>
            </div>
        </div>
        <div class="login-panel">
            <div class="login-header">
                <h1>RPConnect</h1>
            </div>
            
            <form class="login-form">
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" class="form-control" placeholder="your.id@email.com" required>
                </div>
                
                <div class="form-group">
                    <label for="password">PASSWORD</label>
                    <input type="password" id="password" class="form-control" placeholder="••••••••" required>
                </div>
                
                <div class="button-group">
                    <button type="submit" class="btn-login">LOGIN</button>
                    <button type="button" class="btn-register" id="registerBtn" onclick="window.location.href='/register'">CREATE ACCOUNT</button>
                    <div class="forgot-link"><a href="/forgetPassword">Forgot Password</a></div>
                </div>
            </form>
            
        </div>
    </div>
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Create dynamic tech circles
            const techPanel = document.querySelector('.tech-panel');
            for (let i = 0; i < 3; i++) {
                const circle = document.createElement('div');
                circle.classList.add('tech-circle');
                
                const size = Math.random() * 100 + 50;
                const posX = Math.random() * 100;
                const posY = Math.random() * 100;
                const opacity = Math.random() * 0.1 + 0.05;
                
                circle.style.width = '${size}px';
                circle.style.height = '${size}px';
                circle.style.left = '${posX}%';
                circle.style.top = '${posY}%';
                circle.style.opacity = opacity;
                circle.style.background = 'rgba(0, 99, 65, ${opacity})';
                
                techPanel.appendChild(circle);
            }
            
            // Form submission
            const form = document.querySelector('.login-form');
            form.addEventListener('submit', function(e) {
                e.preventDefault();
                const email = document.getElementById('email').value;
                const password = document.getElementById('password').value;
                
                if (email && password) {
                    // Simulate loading
                    const btn = form.querySelector('.btn-login');
                    btn.textContent = 'Authenticating...';
                    btn.style.backgroundColor = '#004d33';
                    
                    setTimeout(() => {
                        btn.textContent = 'Access Granted';
                        btn.style.backgroundColor = '#006341';
                        alert(`Welcome to RPConnect\n\n:User  ${email}`);
                        setTimeout(() => {
                            window.location.href = "/OTP";
                        }, 1000);
                    }, 1500);
                } else {
                    alert('Please enter both your Enterprise ID and Passcode');
                }
            });
        });
    </script>
</body>
</html>
